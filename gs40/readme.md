# CursorPagination

This cursor-based pagination presents an opaque "cursor" indicator that the client may use to page through the result set. This pagination style only presents formward and reverse controls, and does not allow the client to navigate to arbitrary positions.
